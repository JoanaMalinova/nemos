import { UpperNav } from "../navigation/upper navbar/UpperNav";
import styles from "./Header.module.css";


export const Header = () => {
    return (
        <header>
            <UpperNav />
        </header>
    )
}